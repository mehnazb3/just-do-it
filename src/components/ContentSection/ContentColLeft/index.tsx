import React, {FC} from 'react';
import {ContentColLeftWrapper} from './styles';

const ContentColLeft:FC = () => {
   return(
        <ContentColLeftWrapper>
          Important links and pics
        </ContentColLeftWrapper>
   )
}

export default ContentColLeft;